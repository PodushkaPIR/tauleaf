services:
  tauleaf:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./my-latex-project:/project
    command: ["tauleaf", "-project", "/project", "-main", "main.tex", "-engine", "lualatex"]
    restart: unless-stopped
